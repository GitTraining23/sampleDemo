@Given("user enters email value {string}")
		public void user_enters_email_value(String email) {
			 forgetp.enterEmail(email);
		}

		@Given("user enters mobileno {string}")
		public void user_enters_mobileno(String mobileno) {
			forgetp.enterMobileNo(mobileno);
		    
		}


		//ForgetPassword
		@Given("user clicks ForgetPassword link")
		public void user_clicks_forget_password_link() {
			
		   forgetp.clickForgetPassword();
		}

		

		@When("user clicks reset password button")
		public void user_clicks_reset_password_button() {
		    forgetp.clickResetPassword();
		}

		@Then("user gets success message and verify the message")
		public void user_gets_success_message_and_verify_the_message() {
		   String resetPasswordMsg=forgetp.resetSucMsg();
		   
			Assert.assertEquals(ActualMessage, resetPasswordMsg );

		}
